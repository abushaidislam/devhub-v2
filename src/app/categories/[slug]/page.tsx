import type {Metadata} from "next";import {notFound} from "next/navigation";import {SiteHeader} from "@/components/site-header";import {ToolCard} from "@/components/tool-card";import {categories,tools} from "@/lib/tools";
export function generateStaticParams(){return categories.map(slug=>({slug:slug.toLowerCase()}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const category=categories.find(item=>item.toLowerCase()===slug);if(!category)return {};const items=tools.filter(tool=>tool.category===category);const description=`Browse ${items.length} free, local-first ${category.toLowerCase()} tools in DevHub.`;return {title:`${category} Developer Tools`,description,alternates:{canonical:`/categories/${slug}`},openGraph:{title:`${category} Developer Tools — DevHub`,description,url:`/categories/${slug}`}}}export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const category=categories.find(c=>c.toLowerCase()===slug);if(!category)notFound();const items=tools.filter(t=>t.category===category);return <main><SiteHeader/><section className="page-hero"><div className="container"><span className="label">Category</span><h1>{category}</h1><p>{items.length} focused utilities, built with one consistent interface.</p></div></section><section className="section"><div className="container"><div className="tools-grid">{items.map(t=><ToolCard key={t.slug} tool={t}/>)}</div></div></section></main>}
