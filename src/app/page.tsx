import Link from "next/link";
import { ArrowRight, Command, LockKeyhole, Gauge, Blocks } from "lucide-react";
import { SiteHeader } from "@/components/site-header";
import { ToolCard } from "@/components/tool-card";
import { categories, tools } from "@/lib/tools";

export default function HomePage() {
  const featured = tools.filter((tool) => tool.featured).slice(0, 6);
  return <main><SiteHeader/><section className="hero"><div className="hero-grid"/><div className="container hero-inner"><div className="eyebrow"><span className="pulse"/>Built for the work between the work</div><h1>Developer tools,<br/><span>engineered for speed.</span></h1><p className="hero-copy">Format, convert, inspect and generate—without context switching. A private, keyboard-first toolkit that stays out of your way.</p><div className="hero-actions"><Link className="button primary large" href="/dashboard">Open dashboard <ArrowRight size={16}/></Link><a className="button secondary large" href="https://github.com/Sayed-Saa-new/devhub-toolkit-v2">View source</a></div><div className="command-preview"><Command size={17}/><span>Jump to any tool</span><kbd>⌘</kbd><kbd>K</kbd></div></div></section><section className="section"><div className="container"><div className="section-heading"><div><span className="label">Popular tools</span><h2>Everything you reach for. Instantly.</h2></div><Link href="/dashboard">View dashboard <ArrowRight size={15}/></Link></div><div className="tools-grid">{featured.map((tool)=><ToolCard key={tool.slug} tool={tool}/>)}</div></div></section><section className="section muted" id="principles"><div className="container"><div className="section-heading"><div><span className="label">Designed with intent</span><h2>Fast is a feature.</h2></div></div><div className="principles"><article><Gauge/><h3>Instant by default</h3><p>Server-first rendering, focused bundles and interfaces with no waiting around.</p></article><article><LockKeyhole/><h3>Private by design</h3><p>Local-first processing keeps sensitive payloads in your browser.</p></article><article><Command/><h3>Keyboard native</h3><p>Search, navigate and run workflows without reaching for the mouse.</p></article><article><Blocks/><h3>Built to scale</h3><p>Typed data and composable UI make every new tool consistent.</p></article></div></div></section><section className="section" id="categories"><div className="container"><div className="section-heading"><div><span className="label">Categories</span><h2>One home for every workflow.</h2></div></div><div className="category-list">{categories.map((category)=><Link key={category} href={`/categories/${category.toLowerCase()}`}><span>{category}</span><small>{tools.filter((tool)=>tool.category===category).length} tools</small><ArrowRight size={16}/></Link>)}</div></div></section><section className="cta"><div className="container cta-inner"><div><span className="label">Ready when you are</span><h2>Less searching. More shipping.</h2><p>Open the dashboard and keep your flow intact.</p></div><Link className="button primary large" href="/dashboard">Open dashboard <ArrowRight size={16}/></Link></div></section><footer><div className="container footer-inner"><strong>▲ DevHub</strong><span>Developer utilities for focused work.</span><div><Link href="/dashboard">Dashboard</Link><Link href="/tools">Tools</Link><a href="https://github.com/Sayed-Saa-new/devhub-toolkit-v2">GitHub</a></div></div></footer></main>;
}
