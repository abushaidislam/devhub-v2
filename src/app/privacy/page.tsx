import type {Metadata} from "next";import {TrustPage} from "@/components/trust-page";export const metadata:Metadata={title:"Privacy",description:"How DevHub processes and stores tool data.",alternates:{canonical:"/privacy"},openGraph:{type:"website",url:"/privacy",title:"Privacy — DevHub",description:"How DevHub processes and stores tool data."}};export default function Page(){return <TrustPage title="Privacy" intro="The current tools process their inputs in your browser. DevHub does not send tool input or output to its application server." sections={[{title:"Local processing",body:"Formatting, conversion, generation, and preview operations run in browser code. Network and AI tools will require a visible disclosure before execution if they are added."},{title:"Storage",body:"Favorites store tool identifiers in localStorage. Tool input and output are not persisted by default. Future history will remain optional and provide clear-data controls."},{title:"Analytics",body:"Vercel Analytics may receive page and interaction metadata, but raw tool input, output, JWTs, URLs, SQL, hashes, clipboard content, and user file names must not be included."}]}/>}