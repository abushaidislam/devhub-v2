import type {Metadata} from "next";import {notFound} from "next/navigation";import {DashboardShell} from "@/components/dashboard-shell";import {FavoriteButton} from "@/components/favorite-button";import {JsonLd} from "@/components/json-ld";import {NextActions} from "@/components/next-actions";import {ToolRuntime} from "@/components/tool-runtime";import {site} from "@/lib/site";import {getTool,tools} from "@/lib/tools";import styles from "./tool-page.module.css";export function generateStaticParams(){return tools.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const tool=getTool(slug);return tool?{title:tool.name,description:tool.description,alternates:{canonical:`/tools/${slug}`},openGraph:{title:`${tool.name} — DevHub`,description:tool.description,url:`/tools/${slug}`,type:"website"}}:{}}export default async function ToolPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const tool=getTool(slug);if(!tool)notFound();const jsonLd={"@context":"https://schema.org","@type":"SoftwareApplication",name:tool.name,url:`${site.url}/tools/${tool.slug}`,description:tool.description,applicationCategory:"DeveloperApplication",operatingSystem:"Any",isAccessibleForFree:true,offers:{"@type":"Offer",price:"0",priceCurrency:"USD"}};return <DashboardShell activeSlug={tool.slug}><JsonLd data={jsonLd}/><header className={styles.context} aria-label={`${tool.category}: ${tool.description}`}><div className={styles.contextContent}><div className={styles.contextLine}><span className="label">{tool.category}</span><span className={styles.separator} aria-hidden="true">/</span><p>{tool.description}</p></div></div><FavoriteButton slug={tool.slug}/></header><ToolRuntime slug={tool.slug} name={tool.name}/><NextActions currentSlug={tool.slug}/></DashboardShell>}
