import type {Metadata} from "next";import {TrustPage} from "@/components/trust-page";export const metadata:Metadata={title:"AI data policy",description:"How DevHub bring-your-own-key AI assistance handles data.",alternates:{canonical:"/ai-data-policy"}};export default function Page(){return <TrustPage title="AI data policy" intro="Every DevHub tool runs locally in your browser. AI assistance is optional, disabled by default, and uses a provider you configure yourself." sections={[{title:"Bring your own provider",body:"DevHub ships no AI key and runs no AI server. On the Assistant page you supply an OpenAI-compatible endpoint, a model name, and, when the provider requires one, your own API key. Those values are stored in this browser's local storage only and are sent only to the endpoint you entered. Removing them from the Assistant page or clearing site data deletes them."},{title:"What is sent",body:"Workflow planning sends the goal text you type plus the DevHub tool catalog (tool ids, descriptions, and accepted value types). Error explanation sends the selected tool id and the error message you paste, truncated to 400 characters. Tool input, tool output, favorites, recent activity, and saved recipes are never sent."},{title:"Explicit consent",body:"Each request requires you to tick a consent checkbox that names the destination model and host. The checkbox resets after every request, so no request leaves the browser without a fresh confirmation."},{title:"Local models",body:"Choosing a local endpoint such as Ollama keeps AI requests on your own machine. Hosted providers are labelled as hosted and are never described as local processing."},{title:"No hidden reuse",body:"DevHub does not log, store, or reuse prompts or responses. Retention and training behaviour on the provider side are governed by that provider's own terms, which you should review before sending anything sensitive."},{title:"Nothing is executed automatically",body:"A proposed workflow is validated against the local engine registry and shown to you as a list of steps. It only runs after you save it as a recipe and run it yourself, entirely in the browser."}]}/>}
