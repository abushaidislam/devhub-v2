import type {Metadata} from "next";import {TrustPage} from "@/components/trust-page";export const metadata:Metadata={title:"Documentation",description:"How DevHub works and what its tools guarantee.",alternates:{canonical:"/docs"}};export default function Page(){return <TrustPage title="Documentation" intro="DevHub is a free, local-first workspace for recurring developer transformations." sections={[{title:"Using tools",body:"Choose a tool, enter data, select an operation when available, then run it. Results can be copied or downloaded when the format supports it."},{title:"Processing model",body:"The current 24 tools execute deterministic processing in the browser. Favorites contain tool identifiers only. Inputs and outputs are not saved by default."},{title:"Tool boundaries",body:"Validation messages describe malformed input. JWT decoding does not verify signatures, SQL and cron support are intentionally basic, and Markdown supports a documented safe subset."}]}/>}