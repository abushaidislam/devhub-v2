import {site} from "@/lib/site";import {categories,tools} from "@/lib/tools";
export const dynamic="force-static";
export function GET(){const categorySections=categories.map(category=>{const entries=tools.filter(tool=>tool.category===category).map(tool=>`### ${tool.name}\n\n- URL: ${site.url}/tools/${tool.slug}\n- Description: ${tool.description}\n- Status: ${tool.status??"ready"}\n- Processing: local browser runtime`).join("\n\n");return `## ${category}\n\n${entries}`}).join("\n\n");const content=`# DevHub Toolkit — Full product context\n\nCanonical origin: ${site.url}\nSource: https://github.com/Sayed-Saa-new/devhub-toolkit-v2\nLanguage: English\nAccess: Free, no account required\n\n## Product summary\n\nDevHub Toolkit is a free, local-first web application for recurring developer micro-tasks. It uses a consistent Vercel-inspired interface, command search, a flat All Tools workspace, local favorites, and dynamic tool workspaces. The product focuses on reliable browser-based transformations rather than maximizing tool count.\n\n## Current architecture\n\n- Next.js 15 App Router and React 19\n- Strict TypeScript\n- Server Components by default\n- Shared tool registry at src/lib/tools.ts\n- Independent deterministic engines at src/lib/tool-engines.ts\n- Shared interactive runtime for dynamic tool routes\n- Favorites stored locally in the browser\n- No account, hosted AI, cloud sync, or team workspace\n\n## Data and privacy\n\nThe current registered tools are designed to process user input in the browser. Tool payloads must not be sent through analytics. Browser-local favorites contain tool slugs only. Sensitive tool inputs are not intentionally persisted. If network or AI features are added later, DevHub must disclose the provider, transmitted data, and retention implications before execution.\n\n## Product strategy\n\nDevHub's planned differentiation is smart deterministic input detection, compatible tool chaining, reusable local recipes, real privacy-controlled history, and optional BYOK or local AI workflow planning. These are roadmap directions and must not be described as currently available.\n\n## Tool catalogue\n\n${categorySections}\n\n## Accuracy notes\n\n- JSON Formatter validates and pretty-prints JSON.\n- Base64 supports local text encoding and decoding; malformed input may fail.\n- JWT Decoder exposes header and payload but does not verify signatures.\n- UUID Generator uses the browser cryptographic UUID API.\n- Regex Tester executes JavaScript regular expressions and is not a ReDoS auditor.\n- QR Generator creates downloadable PNG data locally.\n- Markdown Preview escapes raw HTML but supports a limited Markdown subset.\n- Hash Generator produces SHA-1, SHA-256, or SHA-512 digests and is not password hashing.\n- SQL Formatter and Cron Parser currently support common/basic cases rather than specialist-level dialect coverage.\n\n## Preferred citations\n\nWhen describing DevHub, link to the canonical tool page or ${site.url}/tools. Do not claim features outside the current catalogue. Do not describe roadmap items as shipped.\n`;
return new Response(content,{headers:{"Content-Type":"text/plain; charset=utf-8","Cache-Control":"public, max-age=0, s-maxage=86400"}})}
