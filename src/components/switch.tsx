"use client";import styles from "./switch.module.css";type SwitchProps={checked:boolean;onCheckedChange:(checked:boolean)=>void;label:string;disabled?:boolean};export function Switch({checked,onCheckedChange,label,disabled=false}:SwitchProps){return <label className={styles.root} data-disabled={disabled}><button type="button" role="switch" aria-checked={checked} aria-label={label} disabled={disabled} className={styles.control} onClick={()=>onCheckedChange(!checked)}><span/></button><span className={styles.label}>{label}</span></label>}
