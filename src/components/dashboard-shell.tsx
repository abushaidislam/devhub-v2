"use client";
import {useEffect,useMemo,useState,type ReactNode} from "react";import Link from "next/link";import {usePathname} from "next/navigation";import {ArrowLeftRight,BookOpen,Braces,ChevronRight,ChevronsUpDown,Clock3,FileCode2,Github,Grid2X2,Heart,Menu,MoreHorizontal,Palette,PanelLeftClose,PanelLeftOpen,Search,Shield,Shapes,Sparkles,Type,Workflow,X} from "lucide-react";import {CommandPalette} from "./command-palette";import {categories,tools} from "@/lib/tools";import {recordHistory} from "@/lib/history";import {useFavorites} from "@/lib/use-favorites";import styles from "./dashboard-shell.module.css";const categoryIcons={Formatters:Braces,Converters:ArrowLeftRight,Security:Shield,Generators:Sparkles,Text:Type,Design:Palette,Editors:FileCode2,Reference:BookOpen} as const;export function DashboardShell({children,activeSlug}:{children:ReactNode;activeSlug?:string}){const pathname=usePathname();const [mobileOpen,setMobileOpen]=useState(false);const [sidebarOpen,setSidebarOpen]=useState(true);const [commandOpen,setCommandOpen]=useState(false);const {favorites}=useFavorites();const activeCategory=useMemo(()=>tools.find(tool=>tool.slug===activeSlug)?.category,[activeSlug]);const pageTitle=pathname==="/favorites"?"Favorite tools":pathname==="/recent"?"Recent tools":pathname==="/recipes"?"Saved recipes":pathname==="/assistant"?"AI assistant":activeSlug?(tools.find(tool=>tool.slug===activeSlug)?.name??"Tool"):"All tools";useEffect(()=>{const open=()=>setCommandOpen(true);window.addEventListener("devhub:command",open);return()=>window.removeEventListener("devhub:command",open)},[]);useEffect(()=>{if(activeSlug)void recordHistory(activeSlug)},[activeSlug]);return <div className={`${styles.app} ${!sidebarOpen?styles.noSidebar:""}`}><aside className={`${styles.sidebar} ${mobileOpen?styles.open:""}`}><div className={styles.workspaceSwitcher}><img className={styles.workspaceLogo} src="/icon.png" alt="" width={22} height={22} aria-hidden/><div className={styles.workspaceIdentity}><strong>DevHub</strong><span>Toolkit</span></div><ChevronsUpDown size={15}/><button aria-label="Close navigation" onClick={()=>setMobileOpen(false)}><X size={17}/></button></div><nav className={styles.primary} aria-label="Workspace navigation"><Link href="/dashboard" data-active={pathname==="/dashboard"}><span className={styles.primaryIcon}><Grid2X2 size={18}/></span>All tools<small>{tools.length}</small></Link><Link href="/favorites" data-active={pathname==="/favorites"}><span className={styles.primaryIcon}><Heart size={18} fill={pathname==="/favorites"?"currentColor":"none"}/></span>Favorites<small>{favorites.length}</small></Link><Link href="/recipes" data-active={pathname==="/recipes"} onClick={()=>setMobileOpen(false)}><span className={styles.primaryIcon}><Workflow size={18}/></span>Recipes</Link><Link href="/assistant" data-active={pathname==="/assistant"} onClick={()=>setMobileOpen(false)}><span className={styles.primaryIcon}><Sparkles size={18}/></span>Assistant</Link><Link href="/recent" data-active={pathname==="/recent"} onClick={()=>setMobileOpen(false)}><span className={styles.primaryIcon}><Clock3 size={18}/></span>Recent</Link></nav><div className={styles.divider}/><div className={styles.scroll}><div className={styles.sectionLabel}>Tool categories</div>{categories.map((category,index)=>{const CategoryIcon=categoryIcons[category as keyof typeof categoryIcons]??Shapes;const categoryTools=tools.filter(tool=>tool.category===category);return <details key={category} open={activeCategory?activeCategory===category:index===0}><summary><span className={styles.primaryIcon}><CategoryIcon size={18}/></span><span>{category}</span><small>{categoryTools.length}</small><ChevronRight className={styles.chevron} size={15}/></summary><div className={styles.toolChildren}>{categoryTools.map(tool=>{const Icon=tool.icon;return <Link key={tool.slug} href={`/tools/${tool.slug}`} data-active={activeSlug===tool.slug} onClick={()=>setMobileOpen(false)}><span className={styles.toolIcon}><Icon size={14}/></span><span>{tool.name}</span>{favorites.includes(tool.slug)&&<Heart className={styles.favoriteMark} size={11} fill="currentColor"/>}</Link>})}</div></details>})}</div><footer><div className={styles.avatar}>D</div><div><strong>DevHub workspace</strong><small>Local-first tools</small></div><a href="https://github.com/Sayed-Saa-new/devhub-toolkit-v2" aria-label="GitHub repository"><Github size={15}/></a><button aria-label="More options"><MoreHorizontal size={16}/></button></footer></aside>{mobileOpen&&<button className={styles.backdrop} aria-label="Close navigation" onClick={()=>setMobileOpen(false)}/>}<div className={styles.main}><header className={styles.topbar}><div><button className={styles.menu} aria-label="Open navigation" onClick={()=>setMobileOpen(true)}><Menu size={18}/></button><button className={styles.collapse} aria-label={sidebarOpen?"Hide navigation":"Show navigation"} onClick={()=>setSidebarOpen(value=>!value)}>{sidebarOpen?<PanelLeftClose size={17}/>:<PanelLeftOpen size={17}/>}</button><Link href="/dashboard" className={styles.crumb}>DevHub <span>/</span> {pageTitle}</Link></div><h1 className={styles.pageTitle}>{pageTitle}</h1><div className={styles.topActions}><button className={styles.search} aria-label="Search tools" onClick={()=>setCommandOpen(true)}><Search size={15}/><span>Search tools…</span><kbd>⌘K</kbd></button><Link href="/" className={styles.home}>Landing page</Link></div></header><main className={styles.content}>{children}</main></div><CommandPalette open={commandOpen} onOpenChange={setCommandOpen}/></div>}
