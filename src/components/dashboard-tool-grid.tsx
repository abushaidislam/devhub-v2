"use client";
import Link from "next/link";import {ArrowUpRight,Heart} from "lucide-react";import {tools} from "@/lib/tools";import {useFavorites} from "@/lib/use-favorites";import styles from "./dashboard-tool-grid.module.css";
export function DashboardToolGrid({favoritesOnly=false}:{favoritesOnly?:boolean}){const {favorites,toggle}=useFavorites();const visibleTools=favoritesOnly?tools.filter(tool=>favorites.includes(tool.slug)):tools;if(favoritesOnly&&visibleTools.length===0)return <div className={styles.empty}><span><Heart size={22}/></span><h2>No favorite tools yet</h2><p>Use the heart icon on any tool card to save it to this workspace.</p><Link href="/dashboard">Browse all tools <ArrowUpRight size={13}/></Link></div>;return <div className={styles.grid}>{visibleTools.map(tool=>{const Icon=tool.icon;const active=favorites.includes(tool.slug);return <article key={tool.slug} className={styles.card}><Link href={`/tools/${tool.slug}`} aria-label={`Open ${tool.name}`}><span className={styles.icon}><Icon size={19}/></span><div><strong>{tool.name}</strong><p>{tool.description}</p><small>{tool.category}<ArrowUpRight size={12}/></small></div></Link><button type="button" data-active={active} aria-pressed={active} aria-label={active?`Remove ${tool.name} from favorites`:`Add ${tool.name} to favorites`} onClick={()=>toggle(tool.slug)}><Heart size={15} fill={active?"currentColor":"none"}/></button></article>})}</div>}
