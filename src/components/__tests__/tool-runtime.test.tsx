import {render,screen,waitFor} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {beforeEach,describe,expect,it,vi} from "vitest";import {ToolRuntime} from "@/components/tool-runtime";
const {toDataURL}=vi.hoisted(()=>({toDataURL:vi.fn()}));vi.mock("qrcode",()=>({default:{toDataURL}}));
describe("ToolRuntime",()=>{beforeEach(()=>{toDataURL.mockResolvedValue("data:image/png;base64,qr")});it("runs, copies, and resets a text tool",async()=>{const user=userEvent.setup();render(<ToolRuntime slug="json-formatter" name="JSON Formatter"/>);await user.click(screen.getByRole("button",{name:/run/i}));expect(screen.getByText("Valid JSON")).toBeInTheDocument();const copyButton=screen.getByRole("button",{name:"Copy output to clipboard"});await user.click(copyButton);expect(screen.getByRole("button",{name:"Copied output to clipboard"})).toBeInTheDocument();await user.click(screen.getByRole("button",{name:/reset/i}));expect(screen.getByText("Run the tool to see the output.")).toBeInTheDocument()});it("shows a safe error and disables copy for invalid input",async()=>{const user=userEvent.setup();render(<ToolRuntime slug="json-formatter" name="JSON Formatter"/>);const input=screen.getByRole("textbox",{name:"JSON Formatter input"});await user.clear(input);await user.type(input,"{{");await user.click(screen.getByRole("button",{name:/run/i}));expect(screen.getAllByText(/Expected property name|Unexpected token/)).toHaveLength(2);expect(screen.getByRole("button",{name:/copy/i})).toBeDisabled()});it("switches Base64 mode",async()=>{const user=userEvent.setup();render(<ToolRuntime slug="base64" name="Base64"/>);await user.selectOptions(screen.getByRole("combobox",{name:"Operation"}),"decode");const input=screen.getByRole("textbox",{name:"Base64 input"});await user.clear(input);await user.type(input,"RGV2SHVi");await user.click(screen.getByRole("button",{name:/run/i}));expect(screen.getByText("DevHub")).toBeInTheDocument()});it("generates and downloads a QR image locally",async()=>{const user=userEvent.setup();render(<ToolRuntime slug="qr-generator" name="QR Generator"/>);await user.click(screen.getByRole("button",{name:/run/i}));await waitFor(()=>expect(toDataURL).toHaveBeenCalled());expect(screen.getByRole("img",{name:"Generated QR code"})).toBeInTheDocument();expect(screen.getByRole("link",{name:/download png/i})).toHaveAttribute("download","devhub-qr.png");expect(screen.getByRole("button",{name:/copy/i})).toBeDisabled()})});
