// @vitest-environment jsdom
import {render,screen,waitFor} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {describe,expect,it} from "vitest";import {DashboardToolGrid} from "@/components/dashboard-tool-grid";
describe("DashboardToolGrid",()=>{it("shows the favorite empty state",async()=>{render(<DashboardToolGrid favoritesOnly/>);expect(await screen.findByRole("heading",{name:"No favorite tools yet"})).toBeInTheDocument();expect(screen.getByRole("link",{name:/browse all tools/i})).toHaveAttribute("href","/dashboard")});it("shows only locally favorited tools",async()=>{localStorage.setItem("devhub:favorites",JSON.stringify(["jwt-decoder"]));render(<DashboardToolGrid favoritesOnly/>);expect(await screen.findByRole("link",{name:"Open JWT Decoder"})).toBeInTheDocument();expect(screen.queryByRole("link",{name:"Open JSON Formatter"})).not.toBeInTheDocument()});it("favorites a tool from the all-tools card",async()=>{const user=userEvent.setup();render(<DashboardToolGrid/>);const button=await screen.findByRole("button",{name:"Add JSON Formatter to favorites"});await user.click(button);await waitFor(()=>expect(button).toHaveAttribute("aria-pressed","true"));expect(JSON.parse(localStorage.getItem("devhub:favorites")??"[]")).toContain("json-formatter")})});
