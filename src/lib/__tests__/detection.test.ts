import {describe,expect,it} from "vitest";import {detectInput,DETECTION_INPUT_LIMIT} from "@/lib/detection";function segment(value:unknown){return btoa(JSON.stringify(value)).replace(/=/g,"").replace(/\+/g,"-").replace(/\//g,"_")}describe("smart input detection",()=>{it.each([["{\"ready\":true}","json-formatter"],[`${segment({alg:"HS256"})}.${segment({sub:"devhub"})}.signature`,"jwt-decoder"],["https://devhub.tools/path?q=json","url-encoder"],["SELECT id FROM users WHERE active = true","sql-formatter"],["0 9 * * 1","cron-parser"],["#ff00aa","color-converter"],["# Title\n\n- one\n- two","markdown-preview"],["RGV2SHVi","base64"]])("detects %s",(input,slug)=>{expect(detectInput(input).some(result=>result.slug===slug)).toBe(true)});it("returns ranked reasons without guessing plain text",()=>{expect(detectInput("hello developer")).toEqual([]);expect(detectInput('{"url":"https://devhub.tools"}')[0]).toMatchObject({slug:"json-formatter",confidence:1,reason:expect.any(String)})});it("rejects oversized input",()=>{expect(()=>detectInput("x".repeat(DETECTION_INPUT_LIMIT+1))).toThrow(/100,000/)})});
