import {beforeEach,describe,expect,it} from "vitest";import {historyEnabled,readHistory,recordHistory,setHistoryEnabled} from "@/lib/history";describe("history storage",()=>{beforeEach(()=>localStorage.clear());it("defaults to opt-out and persists the explicit setting",()=>{expect(historyEnabled()).toBe(false);expect(setHistoryEnabled(true)).toBe(false)});it("fails closed when IndexedDB is unavailable",async()=>{expect(await readHistory()).toEqual([]);expect(await recordHistory("json-formatter")).toBe(false)})});
