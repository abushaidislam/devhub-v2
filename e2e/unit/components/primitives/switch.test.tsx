import {render,screen} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {describe,expect,it,vi} from "vitest";import {Switch} from "@/components/switch";describe("Switch",()=>{it("exposes its state and requests a state change",async()=>{const user=userEvent.setup();const onCheckedChange=vi.fn();render(<Switch checked={false} onCheckedChange={onCheckedChange} label="Save recent tool visits"/>);const control=screen.getByRole("switch",{name:"Save recent tool visits"});expect(control).toHaveAttribute("aria-checked","false");await user.click(control);expect(onCheckedChange).toHaveBeenCalledWith(true)});it("does not change when disabled",async()=>{const user=userEvent.setup();const onCheckedChange=vi.fn();render(<Switch checked={false} onCheckedChange={onCheckedChange} label="History" disabled/>);await user.click(screen.getByRole("switch",{name:"History"}));expect(onCheckedChange).not.toHaveBeenCalled()})});
